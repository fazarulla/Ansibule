- name: play level variable
  hosts: local
  connection: local
  vars_files:
  - name: detail.yaml
  

  tasks:
  - name: print message
    vars:
      organization: "Bellhopat"        
    ansible.builtin.debug:
        msg: " Working: {{ organization }}, area: {{ department }} , designation: {{ roll }}"