- name: type of variable
  hosts: local
  connection: local
  vars:
    organization: "WBG"
    department: "IT"
    roll: "DevOps"
  tasks:
    vars:
      organization:
    - name: print message
      ansible.builtin.debug:
        msg: " Working: {{ organization }}, area: {{ department }} , designation: {{ roll }}"