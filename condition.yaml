
- name: type of variable
  hosts: local
  connection: local
  vars:
    IS_live: false
  tasks:
  - name: check live or not
    ansible.builtin.debug:
       msg: "this is live server"
    when: IS_live