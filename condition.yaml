- name: demo on condition
  hosts: local
  connnection: local
  vars:
    IS_live: true
  tasks:
  - name: check live or not
    ansible.builtin.debug:
        msg: "this is live server"
    when: IS_live