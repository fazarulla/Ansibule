- name: ansible vs shell module
  hosts: local
  connection: local
  tasks:
  - name: command module test
    ansible.builtin.command: cat /etc/passwd | grep root
    registers: users_output
  - name: print the output
      ansible.builtin.debug:
        msg: "{{ users_output}}"