- name: check a nuber is less than 10
  hosts: local
  connection: local
  vars:
    number: 5
  tasks:
    - name: check number
      ansible.builtin.debug:
        msg: "number is less than 10"
      when: number < 10